{
  "name": "koumori-s",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "$(npm bin)/webpack && $(npm bin)/stylus src/stylus/style.styl -o dist/style.css"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^1.8.2"
  },
  "devDependencies": {
    "child-process-promise": "^2.2.1",
    "d3": "^4.13.0",
    "stylus": "^0.54.5",
    "vue": "^2.5.13",
    "webpack": "^3.10.0"
  }
}
